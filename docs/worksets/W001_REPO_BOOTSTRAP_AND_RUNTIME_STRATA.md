# W001 — Repo Bootstrap And Runtime Strata

## Objective
Lock the initial `OxReplay` repo skeleton, runtime strata, and package-boundary intent.

## Scope
1. Root governance docs and local instructions.
2. `docs/spec/*` starter set.
3. Initial working directory layout.
4. First runtime strata map (`Abstractions`, `Bundle`, `Core`, `Diff`, `Explain`, `Distill`, `Governance`, `Conformance`, `DNA ReCalc`).

## Dependencies
1. Foundation Replay doctrine already promoted.
2. `OxReplay` repo created as sibling to the other `Ox*` repos.

## Exit gate
1. Repo skeleton exists.
2. Starter docs exist.
3. Runtime strata are explicitly named.
4. No lane-semantic ownership has been claimed locally.
