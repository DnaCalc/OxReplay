# OxReplay Test Runs

Use this directory for recorded local or CI replay-runtime validation runs when the outputs are useful to retain inside `OxReplay`.

Examples:
1. bundle-validator runs,
2. adapter-capability validation runs,
3. `DNA ReCalc` CLI smoke runs,
4. diff or distillation regression runs.
