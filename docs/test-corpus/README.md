# OxReplay Test Corpus

This directory is the retained corpus for Replay-governed fixtures and reduced witnesses that are local to `OxReplay`.

Expected contents over time:
1. canonical bundle examples,
2. adapter manifest fixtures,
3. replay diff fixtures,
4. witness distillation fixtures,
5. lifecycle and quarantine examples.

Lane-semantic source fixtures should remain traceable back to the authoritative lane repos.
