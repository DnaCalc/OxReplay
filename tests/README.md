# tests

Reserved for unit, scenario, conformance, and retained regression tests aligned to the `OxReplay` runtime strata.

Near-term expectations:
1. crate-local unit tests live beside the Rust crates under `src/`
2. retained fixtures and replay corpus artifacts live under `docs/test-corpus/`
3. retained human-readable run outputs live under `docs/test-runs/`
