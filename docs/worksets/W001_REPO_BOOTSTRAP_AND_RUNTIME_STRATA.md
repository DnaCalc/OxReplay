# W001 — Repo Bootstrap And Runtime Strata

## Objective
Lock the initial `OxReplay` repo skeleton, runtime strata, and package-boundary intent.

## Scope
1. Root governance docs and local instructions.
2. `docs/spec/*` starter set.
3. Initial working directory layout.
4. First runtime strata map (`Abstractions`, `Bundle`, `Core`, `Diff`, `Explain`, `Distill`, `Governance`, `Conformance`, `DNA ReCalc`).

## Dependencies
1. Foundation Replay doctrine already promoted.
2. `OxReplay` repo created as sibling to the other `Ox*` repos.

## Exit gate
1. Repo skeleton exists.
2. Starter docs exist.
3. Runtime strata are explicitly named.
4. No lane-semantic ownership has been claimed locally.

## Expected capability impact
1. none; `W001` is bootstrap doctrine and repo-shape work only.

## Expected pack impact
1. none; `W001` makes no pack claim.

## Environment Preconditions
1. `Foundation` replay doctrine is already promoted.
2. `OxReplay` exists as a sibling repo to the other `Ox*` repos.
3. No implementation-stack declaration is required because `W001` is doc-first and non-runtime.

## Evidence Layout
1. canonical emitted artifact root: repo-root governance docs and `docs/` work products in place
2. checked-in versus ephemeral policy: checked-in only; no separate replay artifact root is expected
3. stable naming policy for baseline runs: not applicable; `W001` emits no replay runs

## Replay-Corpus Readiness
1. no replay classes are part of the `W001` gate model
2. no corpus scenarios are required
3. replay-corpus planning begins in `W002`

## Pack-Evidence Traceability
1. pack name: none
2. replay classes: none
3. scenario ids or artifact paths: not applicable
