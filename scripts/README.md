# scripts

Local developer scripts and repeatable repo tasks such as validation wrappers, bootstrap helpers, and report generation.

Current planned stable entrypoints:
1. `meta-check.ps1`
   - Rust workspace validation floor (`fmt`, `clippy`, `test`)
